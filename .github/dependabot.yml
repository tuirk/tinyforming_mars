version: 2
updates:
  # Version bumps for npm stay off. CVE fixes still come from Dependabot
  # security updates (GitHub Settings), which do not need this file.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
